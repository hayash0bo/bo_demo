# 商談書き起こし構造化解析デモアプリ 仕様書

## 概要

商談の書き起こしテキストを構造化解析するプロセスをデモする簡易Webアプリ。
「ただの要約」と「構造化解析」の違いを潜在顧客に体感させることが目的。

## 技術スタック

- **フレームワーク**: Next.js (App Router)
- **UI**: React + Tailwind CSS
- **LLM**: OpenAI API (gpt-4.1)
- **状態管理**: React state (DB不要、フロントエンド完結)
- **デプロイ**: ローカル開発 → 将来的にVercel

## 画面構成

4ステップのウィザード形式。画面上部にステップインジケーターを表示し、各ステップ間を自由に行き来できる。

---

### Step 0: サンプル書き起こし生成

**目的**: デモ用の商談書き起こしテキストをLLMで即興生成する。

**入力フォーム**:
| フィールド | 型 | 必須 | 例 |
|---|---|---|---|
| 会議タイトル | テキスト | Yes | 「製造ライン自動化提案」 |
| 業界 | テキスト | Yes | 「自動車製造」 |
| 商材 | テキスト | Yes | 「産業用ロボットアーム」 |
| 営業側参加者 | テキスト(カンマ区切り) | Yes | 「田中太郎, 佐藤花子」 |
| 顧客側参加者 | テキスト(カンマ区切り) | Yes | 「山田部長, 鈴木課長」 |

**生成仕様**:
- 約3000文字の書き起こしテキストを生成
- LLMへのプロンプトに以下の要素を必ず含むよう指示:
  - 顧客の懸念・不安の表明
  - 競合製品への言及
  - 予算・スケジュールに関する議論
  - 次のアクションの合意
  - 顧客の温度感が読み取れる発言
  - ポジティブ/ネガティブ両方の反応

**出力**: 生成された書き起こしテキストをテキストエリアに表示。手動編集も可能。

---

### Step 1: 解析テンプレート定義

**目的**: 書き起こしから抽出する項目と処理方法を定義する。

**プリセット項目** (初期状態で5項目を表示):

| 項目名 | 処理方法 | 処理指示 |
|---|---|---|
| 顧客の温度感 | enum | 選択肢: 高/中/低。顧客の購買意欲・関心度を判定 |
| 商談フェーズ | enum | 選択肢: 初回接触/ニーズ把握/提案/交渉/クロージング |
| 言及された競合 | リスト | 会話中に言及された競合企業・競合製品を列挙 |
| 次のアクション | リスト | 合意された次のステップを列挙 |
| 顧客の懸念点 | リスト | 顧客が表明した懸念・不安・課題を列挙 |
| 主要な議論トピック | リスト | 会議で議論された主要トピックを列挙 |
| 商談要約 | 要約 | 商談内容を3〜5文で要約 |

**処理方法の種類**:
| 処理方法 | 説明 | 追加設定 |
|---|---|---|
| 要約 | 自由文で要約 | 文字数目安(任意) |
| enum | 定義済み選択肢から1つ選択 | 選択肢をカンマ区切りで入力 |
| リスト | 該当項目を列挙 | なし |

**UI操作**:
- 各項目の編集(項目名・処理方法・指示内容)
- 項目の追加(末尾に空行を追加)
- 項目の削除(各行に削除ボタン)
- 項目の並び替え(ドラッグ or 上下ボタン)

---

### Step 2: 解析実行・結果比較

**目的**: 構造化解析の価値を「普通の要約」との比較で示す。

**レイアウト**: 左右2カラム比較表示

| 左カラム: 通常の要約 | 右カラム: 構造化解析 |
|---|---|
| LLMによる自由文要約 | Step 1で定義した項目ごとの解析結果 |

**通常の要約** (左):
- 「この商談の内容を要約してください」というシンプルなプロンプトで生成
- 自由文テキストで表示

**構造化解析** (右):
- Step 1のテンプレートに基づき、項目ごとに解析結果を表示
- enum項目: バッジ/ラベル表示
- リスト項目: 箇条書き表示
- 要約項目: テキスト表示

**処理**: 「解析実行」ボタンで通常要約と構造化解析を並列でAPI呼び出し。

---

### Step 3: ダッシュボード

**目的**: 構造化解析が蓄積されたときの分析価値を示す。

**データ生成方式**:
- Step 2の実際の解析結果1件 + LLMで生成したダミー解析結果9件 = 計10件分のデータでダッシュボードを描画
- ダミーデータはStep 1のテンプレート定義に基づいてLLMが生成(業界・商材のバリエーションを持たせる)

**ダッシュボード構成** (Step 1の定義に応じて動的生成):

| 処理方法 | 表示ウィジェット |
|---|---|
| enum | 円グラフ(割合表示) |
| リスト | 頻出ワードランキング or 一覧テーブル |
| 要約 | VoC(Voice of Customer)一覧カード |

**レイアウト**:
- 上部: KPIサマリー(総商談数, 温度感分布など)
- 中部: enum項目のチャート群
- 下部: リスト・要約項目のテーブル/カード

**チャートライブラリ**: Recharts

---

## API設計

バックエンド不要。フロントエンドからOpenAI APIを直接呼び出す。

**APIキー管理**:
- 画面上部にAPIキー入力欄を配置
- `localStorage`に保存(任意)
- 環境変数 `NEXT_PUBLIC_OPENAI_API_KEY` でも設定可能

**APIコール一覧**:

| ステップ | エンドポイント | 用途 |
|---|---|---|
| Step 0 | `POST /v1/chat/completions` | 書き起こしサンプル生成 |
| Step 2 | `POST /v1/chat/completions` x2 | 通常要約 + 構造化解析 |
| Step 3 | `POST /v1/chat/completions` | ダミー解析データ9件生成 |

**モデル**: `gpt-4.1`

**レスポンス形式**: 構造化解析とダミーデータ生成にはJSON modeを使用。

---

## デザインシステム

### ブランド

- **ロゴ**: BRING OUT (紫〜ピンクグラデーションのキューブアイコン + ワードマーク)
- ロゴは画面左上ヘッダーに配置。画像ファイルを `/public/logo.png` に配置して使用

### カラーパレット

| 用途 | カラー | 値 |
|---|---|---|
| プライマリ(ハイライト) | 紫 | `#7C3AED` (violet-600) → ブランドカラー `#7724EB` |
| プライマリホバー | 濃い紫 | `#6D28D9` (violet-700) |
| プライマリ淡 | 薄紫 | `#EDE9FE` (violet-50) |
| 背景(メイン) | 白 | `#FFFFFF` |
| 背景(セクション) | 薄グレー | `#F9FAFB` (gray-50) |
| 背景(カード) | 白 | `#FFFFFF` (影付き) |
| テキスト(見出し) | 濃いグレー | `#111827` (gray-900) |
| テキスト(本文) | グレー | `#374151` (gray-700) |
| テキスト(補助) | 薄グレー | `#6B7280` (gray-500) |
| ボーダー | 薄グレー | `#E5E7EB` (gray-200) |
| 成功 | 緑 | `#059669` (emerald-600) |
| 警告 | 黄 | `#D97706` (amber-600) |
| エラー | 赤 | `#DC2626` (red-600) |

### タイポグラフィ

- **フォント**: `Inter` (欧文) + システムフォント (和文)
- **見出し**: `font-bold`, gray-900
- **本文**: `font-normal`, gray-700
- **ラベル**: `font-medium text-sm`, gray-500

### コンポーネントスタイル

**ボタン**:
- プライマリ: `bg-[#7724EB] text-white hover:bg-violet-700 rounded-lg px-6 py-2.5`
- セカンダリ: `border border-gray-200 text-gray-700 hover:bg-gray-50 rounded-lg px-6 py-2.5`

**カード**:
- `bg-white rounded-xl border border-gray-200 shadow-sm p-6`

**入力フィールド**:
- `border border-gray-200 rounded-lg px-4 py-2.5 focus:ring-2 focus:ring-[#7724EB]/20 focus:border-[#7724EB]`

**ステップインジケーター**:
- アクティブ: 紫の丸 (`bg-[#7724EB] text-white`) + 紫のラベル
- 完了: 紫チェックマーク付き丸
- 未到達: グレーの丸 (`bg-gray-200 text-gray-500`)
- ステップ間の線: 完了=紫, 未到達=グレー

**バッジ(enum表示)**:
- `bg-violet-50 text-[#7724EB] rounded-full px-3 py-1 text-sm font-medium`

**ヘッダー**:
- 白背景 + 下ボーダー (`border-b border-gray-200`)
- 左: ロゴ
- 右: APIキー入力(小さめ)

### レイアウト

- 最大幅: `max-w-6xl mx-auto`
- セクション間余白: `space-y-8`
- カード内余白: `p-6`
- 全体背景: `bg-gray-50`、カードは `bg-white`

---

## 画面共通仕様

- **レスポンシブ**: デスクトップ優先(デモ用途)。最低幅1024px想定
- **ローディング**: API呼び出し中はスピナー(紫) + ストリーミング表示(可能な範囲)
- **エラーハンドリング**: APIエラー時はトースト通知で表示
- **言語**: 日本語UI

---

## ディレクトリ構成

```
bo_demo/
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx          # メインページ(ウィザード)
│   │   └── globals.css
│   ├── components/
│   │   ├── StepIndicator.tsx  # ステップナビゲーション
│   │   ├── ApiKeyInput.tsx    # APIキー入力
│   │   ├── Step0Generate.tsx  # サンプル生成
│   │   ├── Step1Template.tsx  # テンプレート定義
│   │   ├── Step2Analysis.tsx  # 解析実行・比較
│   │   └── Step3Dashboard.tsx # ダッシュボード
│   ├── lib/
│   │   ├── openai.ts          # OpenAI API呼び出し
│   │   ├── prompts.ts         # プロンプトテンプレート
│   │   └── types.ts           # 型定義
│   └── hooks/
│       └── useAppState.ts     # アプリ全体の状態管理
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── next.config.ts
└── SPEC.md
```
